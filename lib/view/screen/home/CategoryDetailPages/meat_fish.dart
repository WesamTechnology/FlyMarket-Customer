import 'package:flutter/material.dart';


class MeatFish extends StatelessWidget {
  const MeatFish({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("MeatFish"),
      ),
    );
  }
}
