import 'package:flutter/material.dart';


class DairyEggs extends StatelessWidget {
  const DairyEggs({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("DairyEggs"),
      ),
    );
  }
}
